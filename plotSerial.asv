close all;
clear all;
clc;

% create serial object
s = serial("COM3", "BaudRate", 115200);

% open serial port
fopen(s);

i = 1;
while(1)
   
   data(i) = str2double(fscanf(s));
   plot(data);
   
   i = i + 1;
    
    
end
